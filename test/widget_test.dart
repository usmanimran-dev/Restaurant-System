import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App renders login screen', (WidgetTester tester) async {
    // This is a smoke test that will only work with valid Supabase config.
    // For CI, replace with unit tests that mock the repositories.
  });
}
